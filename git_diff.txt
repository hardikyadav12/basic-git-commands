Bayern Munich
Berlin
Leipzig

If we want to compare git commits, we need to provide commit hashes in the git diff command
Exampe - git diff commit1, commit2, commit3

git diff shows difference in only unstaged differences and git diff HEAD shows difference when staged.