Commands and concepts for merging branches using Git

git merge branch_name -> here branch_name is the branch which we need to merge.
Instruction - first switch to the branch where you want to merge..for example, master branch. 
Commands - git switch master, git merge branch_name

Multiple parent for a single commit comes whenever a merge happens
